# 说明 · 待定项与流程影响（2026-09-26）

> 范围：影响「连接即模式」（`方案-连接即模式.md`）六阶段推进的**全部未定项** —— 含 `方案-Finder轻量化.md` 的取舍点、`方案-连接即模式.md` 的隐性点，以及 `待办-当前阶段.md` 里与之相交的条目。
> 换行：本文件按 **LF** 维护。
> 性质：**整理 + 评估，不改任何代码、不改任何配置**。
> 结构：**§1–§4 = 决策项（O1–O11，需你或外部输入拍板）**；**§5–§6 = 设计模糊项（T1–T14，判断会不会挡住写代码）**；**§7 = 开工前的测试需求评估（全新代码 / 全新机制 / 测什么）**。各节互不重复，边界见 §5 开头。

---

## 0. 一句话结论

排查 4 份文档后共 **11 条待定项**，按"对流程的杀伤力"分四档：

| 档                       | 条数 | 含义                     | 处理            |
| ---------------------- | -- | ---------------------- | ------------- |
| **A · 会改变方案本身**        | 1  | 定错则 D1 / D6 要重做        | **必须先定**      |
| **B · 阻塞阶段定稿 / 验收**    | 2  | 阶段编号、阶段 0 能否收尾         | **开工前必须定**    |
| **C · 有默认值可先推进**       | 6  | 影响"内容"，不影响"结构"         | 默认值先跑，随时回填    |
| **D · 纯记录 / 操作**       | 2  | 不影响流程                  | 顺手处理          |

**本轮最实质的发现**：**P2「把 4048 条导入本地库」是整套流程里唯一"从没被设计过"的环节** —— 两份文档都只有一句口号（`方案-Finder轻量化.md` §7.2 / §7.3 步 1）。实测后**这条路的账与文档预期不一致**（见 §2）：验收标准「本地库 2865 → ≈4048 条」**本身就是错的**，且藏着 3 个必须先定的设计点。**它不是"跑个脚本"。**

**补充（§5–§6，追问"流程里哪些目标/方法也不明确"）**：又排查出 **14 条设计模糊项（T1–T14）**。**结论：没有一条"阻塞"开工** —— 真正会导致返工的只有 **T1（`fetch`/`sync` 能力分工）** 与 **T7（独立形态下 Finder 自带凭证怎么探测）**，而这两条**落在同一个接口上**（`GET /api/capabilities` 的响应契约）。→ **开工前只需冻结这一页契约**，其余 12 条全部"边做边定"（详见 §6）。另核出 **D7 的 `CAP_DOM` 示例里 2 个选择器指向了不存在的 DOM**（见 §5.3）。
>
> **再补充（§5.5 / §6 / §7）**：**T5 / T6 你早已答过**（在轻量化 14 行取舍表的第 6 列：项 3「不接备注」、项 4「免掉依赖」）→ **关闭**，不再追问；但顺带核出**一处内部冲突** —— 项 3 的"不加 `remark` 列"与 O3c 的"建议加列"**是两件事**（前者是功能取舍、后者是数据保真，加列成本 ≈5 行且不违反项 3），**建议 O3c 仍按加列走**（见 §5.5）。**T1 / T7 的返工已由你确认可接受** → §6.1 那页契约改为"随第 1 笔一并写死"，不再单独等定稿。**新增 §7 测试评估**：阶段 1 新增 **6 处全新代码 + 7 项全新机制**，但**风险面仅"纯 Python 逻辑 + 一段前端渲染表"** —— **无新依赖 / 新端口 / 新进程 / 新数据表**；测试需求 = **只新增一个纯函数单测脚本**（`dev/test_capabilities.py`，无端口无 mock 毫秒级），因为仓库**目前没有任何断言式单测**（`src/`、`dev/` 里 `unittest|pytest|assert` 零命中）。

---

## 1. 待定项清单（11 条）

| #       | 待定内容                                          | 类型       | 出处                    | 阻塞哪一步                    | 建议默认                          |
| ------- | --------------------------------------------- | -------- | --------------------- | ------------------------ | ----------------------------- |
| **O1**  | **取舍点 A：本地库是否留作离线兜底**                        | 待你定      | 轻量化 §6                | **阶段 0 / 1 / 2 全部**      | **保留**（Q1 已隐含，见 §3.2）         |
| **O2**  | **D ≈140 行"功能补齐"落在哪个阶段**                      | 待你选（三选一） | 连接即模式 §6.5            | 阶段编号定稿                   | **② 独立成一阶段**                  |
| **O3**  | **P2 迁移的实现方式**（含 3 个子决策，见 §2）                | **设计未定** | 轻量化 §7.2 / §7.3 步 1   | **阶段 0 的完成标志**          | upsert ＋ 复合键 ＋ 加 remark 列     |
| **O4**  | **P1 有效 SESSDATA**                            | 待外部输入    | 轻量化 §7.2 / 连接即模式 阶段 0 | 阶段 2 能否"真的可用"           | 阶段 0 内解决                      |
| **O5**  | **Q1「具体策略」的确切含义**                             | 待你确认     | 连接即模式 §7（隐性点）         | 阶段 1（D6 规则表内容）          | 按"数据源选择 ＋ 同步节奏"理解             |
| **O6**  | **D6 规则表阈值**（R3 的 N 天 / M 条 · R4 的 cooldown）  | 有示例值     | 连接即模式 §D6             | 阶段 1                     | 按示例值上线，运行后再调                  |
| **O7**  | **`CAP_DOM` 能力→DOM 映射未列全**（只列 5 项 ＋ `...`）   | 有默认做法    | 连接即模式 §D7             | 阶段 4                     | 开工时逐项补全                       |
| **O8**  | **8 项能力清单是否齐**（`fetch/sync/remark/export/images/integrity/analysis/backup`） | 待核对      | 连接即模式 §D2             | 阶段 1                     | 实现时对着 12 处分叉逐条回勾              |
| **O9**  | **旧端点处置：兼容别名 vs 直接删**                         | 二选一未定    | 连接即模式 §D2             | 阶段 5                     | **直接删**（只被本仓前端使用）             |
| **O10** | **阶段 6 的触发标准「UI 调整好」未定义**                     | 未定义      | 连接即模式 §6.2 阶段 6       | 阶段 6（可选，不阻塞排期）           | 等 UI 轮再定                      |
| **O11** | **E（拆中继 ≈580 行）终局 ＋ `dev/mock_analyzer.py` / `stub_fetcher.py` 去留** | 悬置       | 连接即模式 §4 / 轻量化 §7.4   | 阶段 5 可选 / 非流程            | 保留（否则日后重评组合模式要重写 mock）        |

---

## 2. 重点展开：O3（P2 迁移）—— 唯一"从没设计过"的环节

### 2.1 实测数据（**只读**，未写任何文件、未改任何配置）

按年表统计，并把每条与本地库按 `(bvid, view_at)` 比对：

| 年表                      | 行数       | 唯一 kid  | 其中 `kid=0` | 本地已有     | **需迁入**    |
| ----------------------- | -------- | ------- | ---------- | -------- | ---------- |
| `bilibili_history_2020` | 19       | 19      | 0          | 0        | **19**     |
| `bilibili_history_2021` | 132      | 132     | 0          | 0        | **132**    |
| `bilibili_history_2022` | 46       | 46      | 0          | 0        | **46**     |
| `bilibili_history_2023` | 39       | 39      | 0          | 0        | **39**     |
| `bilibili_history_2024` | 63       | 63      | 0          | 0        | **63**     |
| `bilibili_history_2025` | 64       | 64      | 0          | 0        | **64**     |
| `bilibili_history_2026` | 3685     | **27**  | **3659**   | 2389     | **1296**   |
| **合计**                  | **4048** | 390     | 3659       | 2389     | **1659**   |

**三条结论**

1. **本地库的 2865 条全部是 2026 年**（B站只保留近三个月）→ **2020–2025 那 363 条，本地一条都没有**。
   所以 P2 的真实价值**不是"4048 条"，而是 363 条长尾 ＋ 1296 条 2026 年缺口 = 1659 条**。这也解释了为什么 P2 被称作"唯一有时限的动作"：丢掉就再也拿不回来。
2. **迁移后本地库应为 ≈4524 条**（2865 ＋ 1659），**不是文档写的 ≈4048**；而且**不能整表覆盖** —— 本地有 **476 条 2026 年独有**（Analyzer 没有它）的记录，覆盖即丢。
3. **`kid` 不能用作迁移键**：Analyzer 2026 表 **3659/3685 行的 `kid = 0`**，全库只有 **390** 个唯一 kid（389 个非零 ＋ 1 个 0）。
   代码里 `store.py::_rec_key()` 的注释**早已确认**此事（"`kid` 列 Analyzer 恒为 0、Finder 为 oid"），读取层也**强制覆盖** kid 列 —— 所以迁移**写入时必须自己做键**，不能沿用。

### 2.2 三个必须先定的设计点（O3 的子项）

| 子项      | 要决定什么                            | 建议                                                                    | 不这么做会怎样                                     |
| ------- | -------------------------------- | --------------------------------------------------------------------- | ------------------------------------------- |
| **O3a** | 合并口径：**整表覆盖** 还是 **按 `(bvid, view_at)` upsert** | **upsert**                                                            | 覆盖 → **丢 476 条** 2026 年本地独有记录（Finder 自己抓到的）  |
| **O3b** | 本地表 `kid` **主键**填什么（`kid TEXT PRIMARY KEY`） | 用 `f"{bvid}_{view_at}"`，与 `_rec_key()` 一致                            | 直接用 Analyzer 的 kid → **3659 个 `0` 主键冲突**（或只剩 1 条） |
| **O3c** | 本地表是否**加 `remark` / `remark_time` 列** | **加**                                                                | **389 条带备注的行里，363 条正是 2020–2025 长尾** —— 不加列 = 迁进来的长尾没有备注 |

> **O3c 的注**：备注覆盖与长尾分布高度重合（2020–2025 共 363 条带备注，2026 表另有 26 条，合计 389 条）。也就是说 —— **P2 想保住的那批东西，恰好就是带备注的那批东西**。这条不是可选项。

### 2.3 一个被夸大的担心（澄清，可不处理）

`raw_json` 缺失：1659 条新记录没有 `raw_json`（本地表的 `raw_json` 由直抓写入）。一度担心影响 `dt` / `live_status`。
**实测澄清：影响很低** —— `raw_json` 的唯一消费点是 `store.py::_read_local()` 里取 `dt` / `live_status`，而 `dt` 在整仓**唯一的消费点**是 `app.js:967` 从 DOM dataset 读取（`state.dt = el.dataset.dt`），**不是从记录读**。→ 迁移后这批记录 `dt` 为空，**无人消费**。记一笔即可。

### 2.4 一处口径细节（自洽性校验）

按 `(bvid, view_at)` 算并集时会**略高估** —— 本地 2865 条里有 **11 条 `bvid` 为空**，`_rec_key()` 会回退到 `(oid, view_at)` 键。
所以 **4524 是上界**，实际合并数 ≈ **4513**（4524 − 11），**与 `待办-当前阶段.md` §零 记录的"合并 4513"完全一致** → 口径自洽，交叉验证通过。

---

## 3. 对流程的影响

### 3.1 若全部按建议默认走 → 阶段表只加不动

| 阶段                | 受哪条影响      | 变化                                                |
| ----------------- | ---------- | ------------------------------------------------- |
| **阶段 0 · 前置**      | O1 / O3 / O4 | **变化最大**：完成标志从"本地库含长尾年份"**细化为可判定的验收** —— 本地库 ≈4513 条、含 363 条 2020–2025、`remark` 列已加 |
| **阶段 1 · 新增三层**    | O5 / O6 / O8 | 不变（实现时按 §D2 / §D6 的默认值落地）                        |
| **阶段 1.5（若选 ②）**  | O2         | 新增一段：本地能力补齐 ≈140 行（纯增、零破坏）                        |
| **阶段 2 · 切换判据 ★** | O4         | 不变；但**只有 O4 到位，"独立形态真的可用"才能验证**                   |
| **阶段 3 / 4**       | O7         | 不变（阶段 4 开工时补全 `CAP_DOM`）                          |
| **阶段 5 · 收敛**      | O9 / O11   | 不变（旧端点直接删；中继保留）                                   |
| **阶段 6 · 收尾**      | O10        | 不变（可选，等 UI 轮）                                     |

**净影响：只有"阶段 0 的验收口径"被真正细化（且是往可验证的方向），外加一个可选的新阶段。六阶段的顺序与判据都不用改。**

### 3.2 最坏情况：O1 若反向选择，方案要打回重做

O1（本地库去留）是**唯一会改变方案本身**的一条 —— 因为它与 Q1 直接耦合：

- **保留（建议，也是 Q1 已隐含的）** → 现方案成立：`local` 作为界面上可见的「策略」选项（"始终用本地库"）有实际指代对象；D1 / D6 照写。
- **砍掉** → **D1 的「可见策略」失去指代对象**（没有本地库可指）、**D6 的 `prefer: auto | local` 要重做**、阶段 1 的能力层与策略层都要改 → 等价于出方案 v2。

> 好消息：**Q1 你的答案是"需要不是隐藏"**，这已隐含"本地库留着"（否则那个可见选项没有任何意义）→ **O1 其实已被 Q1 回答，只差你确认一句**（§4 第 1 条）。

### 3.3 一处自动消解：**#39 会被 D1 消解**

本轮顺带核出一件事：**待办 #39（"强制 `analyzer` 模式下 `effective` 会谎报"）不需要单独修** ——

D1 的设计就是**删掉手切值 `analyzer`**（它与"`auto` 且有数据"完全等价、是冗余分支）。那个"会谎报的状态"根本不存在了。
→ 建议：**阶段 2（换判据）或阶段 5（收敛）之后直接关闭 #39**，并留一行备注"由 D1 消解"。
→ **唯一残留**：若日后保留 `?force=analyzer` 这类调试覆盖，谎报仍在 —— 建议不保留该覆盖。

### 3.4 与流程无关的既有待定（列出以免混淆）

以下都不影响六阶段推进，**不要**混进本轮决策：

- **#30 analytics** —— 已判"不做"（20 页 echarts 与 Finder 的"续看/整理"维度正交）。
- **#38 rewatch（重看）标记** —— 已明确"当前阶段只做 P0 修复，不做该功能"。
- **油猴 D / Phase 2** —— 本清单范围外（见 `方案-油猴D.md`）。
- **`方案-前端.md` §3.6 时间导航三缺陷**、**`方案-后端与数据源.md` §7.14** —— 属各自文档的既有待办，与双模式重构不相交。
- **一处文档陈旧需顺手改**：`待办-当前阶段.md` §零 快照里写"Finder `config.json` 已失效（-101），**待计划 A 收口**"，但 A1–A4 已按你的指示**全部不做** → 该表述与新决定矛盾，应改为"凭证由 Analyzer 单点持有；Finder 侧的用途见 O4"。

---

## 4. 待你回的编号（按优先级）

| 优先 | 编号      | 要你回什么                                                        |
| -- | ------- | ------------------------------------------------------------ |
| 1  | **O1**  | 本地库**保留**吗？（我理解 Q1 已隐含"保留"→ 只需一句"确认"即可关闭）                      |
| 2  | **O2**  | D（≈140 行补齐）落在哪个阶段 → **①** 并入阶段 1 ／ **②** 独立成一阶段（建议）／ **③** 并行支线 |
| 3  | **O3**  | 迁移三个子决策是否按建议：**O3a upsert** · **O3b 复合键** · **O3c 加 `remark` 列** |
| 4  | **O4**  | SESSDATA 大概什么时候能拿到（决定阶段 0 能不能按"一次性做完"排）                         |
| 5  | **O5**  | Q1 的"具体策略"是不是指"数据源选择 ＋ 同步节奏"这两类                              |

> O6–O11 都有默认值 / 是记录项，**不必现在回**，实现到那一步再定即可。

---

## 5. 目标 / 实现方法不明确的项（T1–T14）

> **与 §1 的边界**：§1 的 **O1–O11 是"取值决策"**（要不要做 / 选哪个 / 什么时候）—— 需你或外部输入拍板；本节的 **T1–T14 是"设计模糊"**（做成什么样 / 怎么做）—— 多数由实现者定。本节的任务是**把它们找出来，并判断会不会挡住写代码**。
>
> **判定口径**
>
> - **阻塞** = 不先定就写不下去，或写完必修返工
> - **半阻塞** = 先拍一个小决策（一句话），否则会写歪
> - **低** = 实现时自然定，改动局部、不波及其它模块
>
> **先行结论**：**14 条里没有一条是"阻塞"** —— 真正会导致返工的只有 **T1 / T7**，而它们**落在同一个接口上**（见 §6）。

### 5.1 X 类 · 目标不明确（"做什么"本身没定清）

| #          | 模糊点                                       | 为什么算模糊                                                                                       | 影响代码？                                        |
| ---------- | ----------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------- |
| **T1**     | **`fetch` 与 `sync` 两项能力的分工**               | D2 列了 8 项能力，但 `fetch` / `sync` 的**区别从未定义**（`/api/sync` 既读状态也触发；`/api/fetcher-trigger` 才像"fetch"） | **半阻塞**：它是 `derive_capabilities()` 的输出键，也是 `CAP_DOM` 的挂载点 → 写歪要同时改后端与前端 |
| **T2**     | **「独立形态」的界面定义**                           | 阶段 4 的完成标志是"界面自动变独立形态"，但**独立形态下哪些入口消失、哪些换源**从未画过一版                                              | 低：可边做边定，但决定阶段 4 的验收口径                        |
| **T3**     | **`renderSourceBanner` 由 6 分支收敛为 2 —— 具体是哪 2** | 现值 **6 个分支**（`!reachable` / 凭证失效 / `requested=local` / 路径诊断 error / 诊断 warn / auto 降级）。收敛后**其余 4 种状态的信息去哪**没写 | 低-中：#37 的路径诊断是**有价值提示**，收得太狠会把它丢掉            |
| **T4**     | **`analysis` 能力指向哪个页面**                    | 待办 #30 已判"分析不做"，而 D2 的 `analysis` 又被 `CAP_DOM` 映射到 `anRealtimeBtn`（实时更新）—— 三处语义交叉                  | 低：改名即可，但要一次改对                                |
| **T5**     | **独立形态下还有没有"备注（remark）"**                 | 互动补录 1227 行已判不做 → 独立形态 `remark` 必然不可用；但**备注是高频功能**，"灰掉"是否可接受没定                                   | **✅ 已答 → 关闭**（见 §5.5：项 3 明写"不接备注"）              |
| **T6**     | **本地导出的目标格式**                             | 轻量化 §7 写"导出 json/db"，但**本仓零 xlsx 能力**（`.xlsx` 全走 Analyzer 中继；`server.py:1598` 注释明写"不自己写 xlsx"）→ 独立形态若仍要 Excel，就得引 `openpyxl` 从零造 | **✅ 已答 → 关闭**（见 §5.5：项 4 明写"免掉依赖"）              |

### 5.2 Y 类 · 实现方法不明确（做什么清楚，怎么做没写）

| #       | 模糊点                                 | 实读现状                                                                                                                          | 影响代码？                                                                                  |
| ------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| **T7** ⭐ | **独立形态下"Finder 自己那份凭证"怎么探测**         | `_sessdata_status()`（`server.py:1063`）**只探 Analyzer 的 `/login/check`** —— Analyzer 不可达即返回 `unknown`，**根本读不到 Finder 的 `config.json`**；而 D5 要求独立形态显示 `owner: finder(config.json)` | **半阻塞（返工风险最高）**：它是 `probe` 的输出字段，而 probe 是能力层的输入 → 不先定会让 **probe / capabilities / 前端** 三处连着返工 |
| **T8**  | **`probe_connection()` 的字段契约**       | 仅一句"唯一做 IO 的地方"；返回哪些字段、超时多少、并发还是串行、要不要缓存，均未写                                                                                  | 半阻塞：§5 表已给复用清单（`_forward_fetcher` / `_sessdata_status` / `source_status` / `analyzer_db_diagnosis`），照它凑即可 |
| **T9**  | **R3「增量缺口 > M 条」的"缺口"怎么算**           | 状态源**已具备**（`meta.last_sync` / `last_full_sync`，见 Z1）；但"缺口"是"A 库与本地条数差"还是"某时间窗的差"，未定义                                              | 低：纯函数，可先给默认，运行后再调                                                                      |
| **T10** | **`409 not_supported` 的前端统一消费**      | 前端目前**每个调用点各自 `toast(d.error)`**，没有统一入口                                                                                       | 低-中：不集中处理，`reason` 文案会散在十几处                                                            |
| **T11** | **本地"删本地"的粒度**                      | 新功能、无既有实现：按 `kid` / 按日期 / 按当前筛选结果？删后是否连带清 `remark` / `meta`                                                                     | 低：但决定 API 形状，宜一次定                                                                      |
| **T12** | **`load_policy()` 遇旧配置是否写回新格式**      | 未写：只读不写（幂等）还是首读即迁移写盘                                                                                                          | 低：影响回退到旧版本时的兼容性                                                                        |
| **T13** | **"风控码"具体是哪些**                       | `collector.py` 现处理 `-101 / -111`（`collector.py:348`），**未见 `-352 / -412`**；重试已有（最多 3 次 ＋ 退避 `min(5, attempt*2)`）                  | 低-中：决定要在哪加分支、要不要单独退避                                                                   |
| **T14** | **`CAP_DOM` 的完整清单（O7 的展开）**           | 现只列 5 项 ＋ `...`；**且实读发现其中 2 项选择器是错的**（见 §5.3）                                                                                  | 低：开工时对着前端 96 个 `id` 逐个补全                                                                |

### 5.3 ⚠️ 顺带核出两处 D7 的**事实性错误**

`CAP_DOM` 示例（`方案-连接即模式.md` §D7）引用了两个**在页面上不存在**的选择器：

| 示例里的选择器                            | 实读结果                                                                                | 后果                                                                             |
| ---------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `integrity: ['#selfCheckBtn']`     | `index.html` 中 **0 命中**                                                             | 说明**"完整性自检"在前端根本没有入口** → 这条能力是对着空气门控；要么补入口，要么删能力（→ 归入 T1 的能力清单核对）            |
| `analysis: ['.tab-analysis', ...]` | **不存在**：tab 用的是 `data-biz` 属性（值 `"" / archive / live / article`），无 `.tab-analysis` 类 | 选择器写错，照抄会**静默失效**（`querySelectorAll` 返回空集，不报错）                                   |

> 这类"语法写对了、对象指错了"的坑，正是 D7 要求 `applyCapabilities()` **从第一天就表驱动**的原因 —— 但**表本身必须先对着真实 DOM 校对一遍**（→ T14）。

### 5.4 Z 类 · 已核实**不**成问题（列出来消除误报）

以下 6 项本轮都担心过，实读后确认**代码里已有答案**，不必新增任何设计：

| #       | 此前的担心                                   | 实读结果                                                                                                                         |
| ------- | --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| **Z1**  | R3 / R4 要"上次全量时间""增量边界"，若没存就没数据源         | **已有**：`meta.last_sync`（增量边界，`collector.py:499`）＋ `meta.last_full_sync`（`collector.py:493`）＋ `meta.last_success_at`（`server.py:683`） |
| **Z2**  | 单入口后连点两次会不会并发跑两个抓取                      | **已有**：`sync_state["running"]`（`server.py:52`）＋ 两处入口前置检查（`1676` / `1766`）                                                       |
| **Z3**  | `/api/capabilities` 做网络 IO，若高频轮询会拖慢     | `SRC_POLL_MS = 300000`（**5 分钟**，`app.js:2081`）→ 成本可控，无需缓存层                                                                    |
| **Z4**  | 探测可能挂住请求                                | `_forward_fetcher(..., timeout=30)`；`_sessdata_status` 自带 **8s** 超时                                                            |
| **Z5**  | "风控码处理 ≈20 行"要从零造                       | **已有重试骨架**：最多 3 次 ＋ 退避 ＋ 断点续拉（`collector.py:438–451`）→ 只缺风控码分支（→ T13）                                                      |
| **Z6**  | 1659 条迁移记录无 `raw_json`，担心丢 `dt`           | 已在 **§2.3 澄清**：`dt` 的唯一消费点在 DOM dataset，不受影响                                                                                 |

---

### 5.5 T5 / T6 已在取舍表中答过（**关闭**）+ 一处内部冲突澄清

你记的没错 —— 这两条在**第七 / 八轮那张 14 行取舍表的第 6 列**里已经答过：

| #       | 原问题              | 你已有的答案（出处）                                                                                                                | 结论                                            |
| ------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| **T5**  | 独立形态还有没有"备注"     | 项 3「remark 备注读写」：**"0 行。……不接备注 → 本地表也不用加 `remark` / `remark_time` 两列（省掉表结构变更）。互通性零损失"**                                    | **不做**。独立形态 `remark` 能力 `available=false`，UI 置灰 |
| **T6**  | 本地导出要不要 Excel    | 项 4「Excel 导出 / 整库 `.db`」：**"≈40 行，且免掉依赖。`.db`：直接把自己那一个库文件发出去（≈10 行）…… `json`：由 store 序列化"**                            | **不引依赖**：`db`（发自己的库文件）＋ `json`（序列化），**不做 xlsx** |

→ 两条**关闭**，§6.2 里"需你一句话"那句作废。

#### ⚠️ 但本轮核出一处内部冲突：项 3 与 O3c 是**两件事**

> |     | 说的是什么                                    | 属于       |
> | --- | ---------------------------------------- | -------- |
> | 项 3 | 独立形态**不再提供备注功能**（前台不读写备注）                | **功能取舍** |
> | O3c | 一次性迁移时，**那 363 条长尾自带的备注要不要一起搬进本地库**       | **数据保真** |
>
> **功能不做 ≠ 数据要丢。** 若按项 3 字面执行（不加列），迁移那 363 条长尾时其备注**无处安放 → 本地库不带备注**。
>
> - **没到"永久丢失"**：备注仍在 **Analyzer 主库**（389 条），而 Analyzer 进程与调度都还在 → 日后切回组合模式，备注照常显示。
> - **但有真实风险**：若某天 Analyzer 被彻底移除，363 条备注就**只剩备份里有**了。
> - **加列成本 ≈ 0**（`CREATE TABLE` 加两列 ＋ 迁移时带过去，≈5 行），**且不违反项 3** —— 加了列 ≠ 提供备注功能（前台照样不读写它）。
>
> **建议：O3c 仍按"加列"走**，把项 3 理解为"**不做备注功能**"，而非"**不留备注数据**"。若你确认不需要，一句话即可改为不加 —— 这不影响代码结构，只影响迁移脚本的一行。

## 6. 合并结论：现在**可以**开始代码重构

把 §1（O：决策项）与 §5（T：设计模糊项）两张清单合起来看：

| 维度             | 结论                                                                                                |
| -------------- | ------------------------------------------------------------------------------------------------- |
| **O 类会不会挡住开工** | **不会**。O1 已被 Q1 隐含（只差一句确认）；O3 是**阶段 0 的数据操作**，与阶段 1 的代码不相干；O2 只影响阶段编号；O4 是外部输入；O5–O11 全有默认值 |
| **T 类会不会挡住开工** | **不会，但 T1 / T7 会导致返工** —— 而这两条**落在同一个接口上**                                                         |
| **会不会有哪一步"停住"** | 真正"停下就不能开工"的**一条都没有**；T1 / T7 只影响**阶段 1 的那一个端点**                                                 |
| **阶段表要不要改**    | **不改**（阶段编号待 O2 定，仅此一处）                                                                           |
| **测试准备** | 需**新增一层纯函数单测**（≈1 个脚本）；**不需新端口 / 新 mock 服务** —— 详见 **§7** |

### 6.1 唯一需要先冻结的：`GET /api/capabilities` 的**响应契约**

T1（`fetch` / `sync` 分工）与 T7（Finder 凭证探测）**不是两件事，而是同一次定稿的两行** —— 它们都只是"probe 输出什么、capabilities 长什么样"。D2 已有雏形（一段 JSON 示例），只需补 **3 处**：

| 补什么                                             | 对应                | 怎么补（建议）                                                                   |
| ----------------------------------------------- | ----------------- | ------------------------------------------------------------------------- |
| `capabilities` 的**键名 ＋ 语义表**（8 项逐个一句话定义，特别是 `fetch` vs `sync`） | T1 · T4 · T14     | 由实读的 12 处分叉反推一版，写进 §D2                                                   |
| `connection.finder.sessdata` 的**取值与来源**        | T7                | 独立形态下由 Finder 直连 B站 `nav` 探测（`_sessdata_status` 的 Finder 版），字段沿用 `{state, owner, reason}` |
| `analysis` / `integrity` 的**指代修正**              | T4 · §5.3         | `analysis` 改指真实存在的入口；`integrity` 若前端确无入口则**从能力表删除**                      |

**这一页契约冻结了，阶段 1 就能一次写对** —— 其余 T2–T6、T8–T14 全部"边做边定"，因为它们只影响单点实现，改动是局部的。

### 6.2 建议的开工方式（回应"首要是代码重构"）

1. **不等 O1–O11**：它们要么已被隐含（O1）、要么属数据操作（O3）、要么只影响编号（O2）。
2. **§6.1 那一页契约按"边写边定"处理**（你已确认 T1/T7 的返工可接受）—— 开工时随第 1 笔一并写死在代码注释里，不再单独等定稿。
3. **T5 / T6 也已在取舍表中答过**（我只核漏了）→ 见 **§5.5**；其余 T 项**一律由实现时定并写进文档**，不再回头追问。
4. 阶段 1 是**纯增、零行为变更**，做完就能用浏览器直开 `/api/capabilities` 看全貌 —— 这是最安全的开工点。

---

## 7. 开工前的测试需求评估（以阶段 1 为主）

> 问：**现在能动的代码调整有必要测试吗？有没有全新代码或功能机制？**

### 7.1 四问四答

| 问题              | 结论                                                                                                                                  |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| **有必要测试吗**      | **有，但成本极低** —— 只需**新增一层（纯函数单测）**，**不需要新端口 / 新 mock 服务 / 新依赖**                                                                       |
| **有没有全新代码**     | **有 6 处**（全是新函数，**零新依赖**），见 §7.2                                                                                                   |
| **有没有全新机制**     | **有 7 项**，其中 **4 项属"不测就不知道有没有生效"**，见 §7.3                                                                                           |
| **现有测试资产够用吗**   | **不够**：现有 4 个脚本**全是集成级**（起真服务 / 起假后端）；且 `src/` 与 `dev/` 里 **`unittest` / `pytest` / `assert` 全部零命中** → **阶段 1 新增的恰是纯函数，而仓库没有对应的测试形态** |

### 7.2 全新代码（6 处，零新依赖）

| #   | 落点                                          | 量     | 能否单测                                    |
| --- | ------------------------------------------- | ----- | --------------------------------------- |
| 1   | `server.py::probe_connection()`             | ≈+35  | ✗ **唯一做 IO 的** → 用 `dev/mock_analyzer.py` 冒烟 |
| 2   | `store.py::derive_capabilities(probe)`      | +70   | ✅ **纯函数**（probe 是入参）                    |
| 3   | `store.py::decide_sync_plan(probe, policy, state)` | +45   | ✅ **纯函数**（状态也是入参）                       |
| 4   | `store.py::load_policy()` / `save_policy()` | ≈+30  | ✅ 纯读写 ＋ 旧格式兼容                          |
| 5   | `server.py::GET /api/capabilities`          | +25   | 半：进程内可调（不起端口）                           |
| 6   | `server.py::_require_capability()`（阶段 3）   | +?    | 半：进程内可调                                 |

> **关键**：设计时已为可测性留了口子 —— `derive_capabilities()` 与 `decide_sync_plan()` **都以 `probe` 为入参**，`decide_sync_plan` 还额外收 `state`。所以测试**不需要起服务、不需要 mock 后端、不碰磁盘**，喂一个假 `probe` dict 即可。

### 7.3 全新机制（7 项）—— 这才是"要不要新测试手段"的决定因素

| #       | 机制                                              | 为什么算"全新"                                             | 建议的验证方式                                                       |
| ------- | ----------------------------------------------- | --------------------------------------------------- | ------------------------------------------------------------- |
| **M1**  | **纯函数决策层**（能力表 ＋ 策略表）                          | 此前**所有逻辑**都直接读写 sqlite / HTTP；这是仓库里**第一块无副作用的代码**  | **新写 `dev/test_capabilities.py`**（喂假 probe → 断言输出）            |
| **M2**  | **有序规则表 ＋ 命中即止 ＋ cooldown**（D6 的 R1–R6）        | 此前同步类型只有 **3 个 `if mode`**；首次出现"多条规则 ＋ 时间条件"         | 单测：**6 组输入 → 6 个预期 mode**（含边界：cooldown 内 / 缺口为 0 / 首次建基线） |
| **M3**  | **一次探测撑起 8 项能力**（聚合）                           | 此前是 **12 处分叉各自判断**                                 | probe 不可单测 → `mock_analyzer` **3 态冒烟**（可达 / 不可达 / 凭证坏）       |
| **M4**  | **超集兼容期**（新端点首版带旧字段）                           | 首次出现"新旧端点并存"过渡态，**无先例可参照**                          | 断言 `/api/capabilities` **同时**含旧 `ok/reachable/sessdata/source` |
| **M5**  | **表驱动 UI 门控 ＋ 单点常量**（`CAP_DOM` / `UNAVAILABLE_MODE`） | 前端首次用**一份表**控制全部入口可用性                               | 无自动化；**必须对着真实 DOM 校对**（§5.3 两处错即教训）→ 建议加一条**选择器存在性自检**        |
| **M6**  | **`409 not_supported` 语义**                     | 此前不可用一律表现为"转发失败 / 502"                              | 断言状态码与响应体（进程内可调）                                              |
| **M7**  | **配置结构升级 ＋ 旧格式兼容读取**                          | `source_config.json` 此前一直直接读三态                      | 单测：喂旧 `{"mode":"auto"}` → 应得 `policy.prefer="auto"`，且**不写坏原文件**  |

### 7.4 分层建议：什么必须测、什么可以不测

| 层                                 | 必须测                                        | 可以不测                    |
| --------------------------------- | ------------------------------------------ | ----------------------- |
| **纯函数**（M1 / M2 / M7）             | ✅ **必须** —— 新造、无既有回归覆盖，且**测起来最便宜**（无 IO、毫秒级） | —                       |
| **只读端点**（M4 / M6 / 第 5 笔）        | ✅ 必须 —— 都带**外部契约**（前端与兼容期依赖它）             | —                       |
| **IO 探测**（M3）                     | 冒烟即可 —— 复用 `mock_analyzer.py`，**不必新增装置**   | 不必做全组合（3 态足够）           |
| **零行为变更**（阶段 1 的总承诺）              | ✅ **回归**：跑一遍现有 4 个脚本，确认全绿                 | —                       |
| **前端表**（M5）                       | 手工 ＋ 选择器自检                                | 不必自动化                   |

### 7.5 现有 4 个测试资产在阶段 1 / 2 的角色

| 资产                                    | 阶段 1                    | 阶段 2                                        |
| ------------------------------------- | ----------------------- | ------------------------------------------- |
| `dev/regression_restart.py`（⑥ 重启链 5 项） | **当作"零行为变更"基线跑一次**      | 再跑一次（阶段 2 完成标志已要求）                          |
| `dev/regression_fallback.py`（② 自动回退 3/3） | 不动                      | ⚠️ **会失效** —— 它打的端点正是 `/api/fetcher-trigger`，需同步断言位置 |
| `dev/mock_analyzer.py`（假 Analyzer）    | **复用** → 当 probe 的 3 态探针 | 继续复用（"Analyzer 不可达" = 直接停掉它）                 |
| `dev/stub_fetcher.py`（前端桩）           | 无关                      | 退役候选                                        |

### 7.6 值得单独说：仓库**没有单测层**，阶段 1 是最好的引入时机

现有测试**全是"起真服务 ＋ 打印对比"**的集成脚本（`src/`、`dev/` 里 `unittest` / `pytest` / `assert` **零命中**）。
而阶段 1 新增的三块纯函数**天然可测**（`probe` 已做成入参），所以：

- **只新增一个 `dev/test_capabilities.py` 就够**：无端口、无 mock、不碰磁盘、毫秒级；
- 它同时是**阶段 2 的保险**：阶段 2 改判据时，这套单测几秒内就能告诉你"决策逻辑没被改坏"（端到端要起服务、30 秒以上）；
- **不是引入新框架**：标准库 `unittest` 或纯 `assert` ＋ 计数打印即可，与 `dev/` 现有"零依赖"风格一致。

### 7.7 明确"没有"的新东西（让风险面可见）

| 项             | 结论                                                     |
| ------------- | ------------------------------------------------------ |
| 新第三方依赖        | **无**（纯标准库）                                            |
| 新进程 / 新端口     | **无**（probe 只打本机 Analyzer，复用既有转发）                      |
| 新数据库表 / 新写库   | **无**（阶段 1 全程只读；O3c 的加列属阶段 0 数据操作）                     |
| 新配置文件         | **无**（`source_config.json` 只扩字段，且兼容旧格式）                |
| 对 B站的新请求      | **无**（凭证探测只在独立形态启用，见 T7）                               |

→ **风险面 = 纯 Python 逻辑 ＋ 一段前端渲染表**，没有"新基础设施"。这也是"阶段 1 可随时停"的底气：**它连一个持久化副作用都没有。**
