# 待办清单 · 当前阶段（不含 Phase 2 油猴 D）

> 生成：2026-09-25 ｜ **更新：2026-09-25（第二轮）** — 新增「已读」「对应说明」两列，并追加 §五 派生事项。  
> 用途：供人工逐项标注「怎么做 / 优先级 / 是否做」。  
> 范围：仅**当前阶段**（后端 / 前端 / 数据）与**本次对话产生的未完成项**；**不含 Phase 2（油猴 D / `方案-油猴D.md`）**。  
> 用法：在「你的标注」列填写（例：`做` / `不做` / `延后` / 具体做法）。清单外的历史遗留见各 `方案-*.md`。
>
> **本轮约定**：① **不删除任何原有行**；② 原有编号（`#1`–`#19`）**视为稳定 ID，保持不变**（多行互相引用，如 #15 引用 11/12/19）；③ 由你标注**派生出的新事项**统一从 **`#20`** 起编号，列于 §五。
>
> **配套说明文档**（本轮新建，点开即读）：
>
> - `说明-主备架构与数据模式.md` → 对应 #1 / #2 / #10
> - `说明-同bvid多会话策略.md` → 对应 #5
> - `说明-三方能力对照.md` → 对应 #4 / #6 / #7 / #8 / #9

---


## 一、功能/代码类未完成

| #  | 事项                       | 现状        | 出处                  | 备注                                                                                                | 你的标注                                                             | 已读 | 对应说明                                                                                                                                                                                                                                                                                                                                   | 下一步标注 |
| -- | ------------------------ | --------- | ------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | -- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| 1  | **计划 A（双凭证治理）**          | 未执行       | 后端 §7.4 / §7.6      | 停用 `collector.py` 直连、Finder 改纯只读、SESSDATA 只保留 Analyzer `config.yaml` 一处；消除「同步数据」按钮的 `-101` 与双同步路径 | 我觉得可以用Analyzer的，如果没找到Analyzer就需要使用自身能力获取历史，或者检测到失效了可以主动提醒更新，     | ✅  | **采纳**。实读确认：Analyzer **已有** `/login/check-and-notify`（失效发邮件，SMTP 已配）且 `scheduler_config.yaml` **每 10 分钟**跑一次 → **主动提醒不必重造**；Finder 侧只缺「网页可见性」。⚠️ 但**不建议自动回退到自身抓取**（需有效 SESSDATA，与凭证单一化冲突）→ 须**显式开关**。**详见 `说明-主备架构与数据模式.md` §2** ｜ 派生 **#27**                                                                                          |       |
| 2  | **「同步数据」按钮的去留**          | 未定        | 后端 §1.1 / README §3 | 若执行计划 A，该按钮（走 collector）需**改为触发 Analyzer 全量**或直接移除；否则与纯只读定位冲突                                     | 应该来说是原Finder是作为轻量备份存在，主力来说是Analyzer，应该存在一个主开关保证冲突问题，以及2模式的数据规则问题 | ✅  | **已按你的定位出方案**：主开关 `data_source.mode = auto / analyzer / local`（`GET·POST /api/data-source`，切换即 `reload_raw` 不重启）；两模式数据规则对照表（主源/凭证/抓取执行者/写入方向/冲突解决/折叠/skip 键/UI 提示）。**三类冲突**各有处置；关键红利：`kid=(bvid,view_at)` 跨模式一致 → **切模式不丢 skip / views / lists**。**详见 `说明-主备架构与数据模式.md` §3** ｜ 派生 **#21**                                              |       |
| 3  | **② 自动回退分支未现场复现**        | 已编码，未验证   | 后端 §7.2 / §7.4      | 逻辑为「Analyzer 报『未找到本地历史记录』→ 自动转全量」；本地已有基线，无法触发。可选：造空基线环境验证一次                                       | 可放入当前阶段最终待办                                                      | ✅  | **同意**，已承接为 **#20**（列入 §五）。验证方式：临时把 `ANALYZER_DB` 指向一个空库 → 触发 ② → 观察是否自动转全量并在响应里标注回退原因；验完还原。                                                                                                                                                                                                                                           |       |
| 4  | **⑤ 备份的定时/自动与保留策略**      | 仅手动       | 后端 §7.5             | 当前需手点「⑤ 本地备份」；无定时备份、无副本数量/过期清理（`data/backup/` 会无限累积）                                              | 了解Frontend和Analyzer 的策略，保持一样即可                                   | ✅  | **对照结果**：**Analyzer 与 Frontend 都没有自动备份**——Analyzer 仅在 README §233 建议「手动备份 `output` 整个文件夹」；Frontend **完全无备份功能**。→「保持一致」= **不做定时备份**；Finder 的 ⑤ 已是**在线快照**，比二者都强。**实质只剩「保留最近 N 份」清理**。**详见 `说明-三方能力对照.md` §1** ｜ 派生 **#26**                                                                                                              |       |
| 5  | **折叠后旧会话 skip 状态清理**     | 未处理       | 后端 §7.8             | 按 `bvid` 折叠后，被隐藏的旧会话若曾 `manual_skip`，其状态仍留在 `canonical_state.db` 但不显示。**无功能影响**（孤儿记录）；可选清理        | 属于多`bvid的处理策略需要详细方案，目前仅处理了重复显示`                                  | ✅  | **已出详细方案，并实测发现 P0 缺陷**：折叠「只留最新会话」导致 **7 个视频**「历史某次已看完（`progress=-1`）、最近一次没看完」被**误判为需要观看**——这不是显示问题，是**判断错误**，且会随使用持续增长。另：孤儿状态实测 **0 条**（暂无影响）。方案 B「折叠显示 + 状态聚合」（`progress` 取 −1 优先、新增 `session_count`/`first_view_at`/`kids`）可同时修 P0 与 P2。**详见 `说明-同bvid多会话策略.md`** ｜ 派生 **#22**                                                       | 使用方案B |
| 6  | **导出为独立文件（JSON/CSV）**    | 未做        | README §6           | 便携备份/迁移用；目前只有 ⑤ 的 SQLite 快照                                                                       | 了解Frontend和Analyzer 的策略，保持一样即可                                   | ✅  | **对照结果**：Frontend **自己不实现导出**，只转发 Analyzer `/export/*`——可导出 **Excel（年 / 月 / 日期范围）** 与 **整库 `.db` 下载**。→「保持一致」= Finder **中继 3 个端点**（`/export/export_history`、`/export/download_excel/{f}`、`/export/download_db`），**不要自写 Excel 生成**（否则产生第二份口径）。测试入口见说明。**详见 `说明-三方能力对照.md` §2** ｜ 派生 **#25**                                             |       |
| 7  | **remark 备注能力**          | 缺失        | 后端 §7.6 #7          | Analyzer 有 `/history/update-remark` 全套，Finder 无                                                   | 了解Frontend和Analyzer 的策略，是什么样的效果                                  | ✅  | **效果**：给每条历史打一段**自由文本备注**，Frontend 有独立页「我的备注」（卡片=封面+进度条+标题+UP+备注，页头「共 N 条」）。**存储 = Analyzer 主库年表的 `remark` + `remark_time`**，按 `(bvid,view_at)` 定位。**关键**：Finder **已在读 `remark` 字段**（`store.py:37` `ANALYZER_COLS`）→ **展示几乎零成本**；编辑建议**中继** `POST /history/update-remark`（写主库），**不要**存本地侧库。**详见 `说明-三方能力对照.md` §3** ｜ 派生 **#24**       |       |
| 8  | **图片批量下载**               | 缺失（仅按需代理） | 后端 §7.6 #8          | Analyzer 有 `/images/*` 批量下载；Finder 仅 `/cover/` 按需代理                                               | 需要接入Analyzer，并给出测试入口                                             | ✅  | **接入方案 + 测试入口已给出**：中继 5 个端点（`/images/start` · `stop` · `status` · `clear` · `local`）；`use_sessdata=false` 可**规避凭证**（封面/头像属公开内容）。curl 冒烟：`POST /api/images/start?year=2026&use_sessdata=false` → `GET /api/images/status` 看 `downloaded` 递增。另需把 `/cover/` 改为「**本地优先、回退外链**」，否则下了也用不上。⚠️ 属**写盘操作**。**详见 `说明-三方能力对照.md` §4** ｜ 派生 **#23** |       |
| 9  | **分析统计（analytics）**      | 缺失        | 后端 §7.6 #9          | Analyzer 有 `/title/*` `/viewing/*` `/popular/*` `/analysis/*`；Finder 无                            | 了解Frontend和Analyzer 的策略，是什么样的效果                                  | ✅  | **效果**：Analyzer 4 组端点 + Frontend **20 个 echarts 分析页**（数据概览 / 时间分析 / 时间分布 / 月度趋势 / 连续观看 / 最爱重温 / 完成率 / UP主完成率 / 标签 / 时长 / 标题五连 / 热门命中率 / 预测能力 / UP主热门关联 / 分区分布 / 时长分布）。**判断**：与 Finder「续看/整理」维度**正交**，非浏览必需 → **建议不做**，若要则优先**跳转 Analyzer 自带 Web UI**（零维护），而非重画 20 张图。**详见 `说明-三方能力对照.md` §5**                                          |       |
| 10 | **Fetcher API 直连（常新全量）** | 🔲 未规划    | 后端 §0.1             | 可选升级路径；当前走只读直连 Analyzer                                                                           | 目前不算**Fetcher API 直连吗**                                          | ✅  | **澄清：不算直连，属"中继"。** 判据只有两条——**谁向 B站发请求**、**谁持凭证**。②③ 与 ④ 都是 **Analyzer** 在发、**Analyzer** 持凭证 → 是中继。**只有** `POST /api/sync`（走 `collector.py`）才是直连，但凭证**已失效（-101）**。建议文档把该行**改名为「直连 B站抓取（collector 自持凭证）」**，消除歧义。**详见 `说明-主备架构与数据模式.md` §4** ｜ 派生 **#28**                                                                                 |       |

---

## 二、本次改动「待生效 / 待验证」

| #  | 事项                                   | 状态                                  | 出处                                                        | 说明                                                                                                                                                                                                                         | 你的标注 | 已读 | 对应说明                                                                                                                                               |
| -- | ------------------------------------ | ----------------------------------- | --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | -- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 11 | **方案 A：按 `bvid` 折叠（对齐官网）**           | 代码已改，**待重启生效**                      | 后端 §7.8 / `src/store.py`                                  | `reload_raw()` 后 canonical 4450 条（折叠前 4492）；需**重启 8765** 浏览器才可见；前端 `app.js` 无需改                                                                                                                                            | ok   | ✅  | 已确认。**本轮复测**：合并未折叠 **4500** → 折叠后 **4458**（折叠掉 42，涉 37 个 bvid）。⚠️ 文档写的 4450 与实测 4458 属**口径漂移**（见 #18）。**⚠️ 更重要：折叠引入 7 条判断错误 → 见 #5 / #22**         |
| 12 | **`server.py` 连接异常修复**               | 代码已改，**待重启生效**                      | `src/server.py`                                           | `_send` 吞掉 `ConnectionAbortedError/BrokenPipe/ConnectionReset` + `handle_one_request` 兜底；消除刷新/关闭页面时的 WinError 10053 traceback                                                                                              | ok   | ✅  | 已确认。修复已落盘，**待 8765 重启生效**（见 #15）。                                                                                                                  |
| 13 | **文档冲突修复**                           | ✅ 已完成（2026-09-25）                   | 后端 §0/§2/§3/§5、前端 §0/§1/§3/§4/§6、README §2/§3/§4/§5/§6/§7 | 陈旧数字（2813/3278 → 4027/4450）、行数、视图命名、触发端点、README 主源与 SESSDATA 归属、collector 定位、`计划A`/`方案A` 命名消歧                                                                                                                              | ok   | ✅  | 已确认完成。                                                                                                                                             |
| 14 | **⑥ 热重载 / ⑦ 重启服务（网页可控）**             | ✅ 已实现并实测（2026-09-25）→ **已被 #19 取代** | 后端 §7.9 / 前端 §6.1                                         | `POST /api/reload` + `POST /api/restart`（退出码 42 哨兵 → `start.bat` supervisor 自动拉起）；**新增常驻进程 0**。两个端点在 API 层保留                                                                                                               | ok   | ✅  | 已确认。按你的选择合并为 #19 的单入口；两端点**保留在 API 层**未删。                                                                                                          |
| 15 | **一次 `start.bat` 启动使 11/12/19 全部生效** | ⏳ **仍未执行**                          | 本次                                                        | 8765 **已在跑**（PID 21480，启动于 19:12:00），但它是**本次改动前**的旧代码 → `GET /api/code-status` 返回 404。**双击 `start.bat` 重启一次**后，此后改代码只需网页点 ⑥                                                                                                | ok   | ✅  | **你标 ok，但实测该步仍未执行**——8765 现仍是旧代码，`/api/code-status` 会 404。**这是 11/12/19 全部生效的唯一前置**：双击一次 `start.bat` 即可（进入 supervised 模式后就不用再重开 bat）。              |
| 19 | **⑥ 单入口「应用变更」+ 自动判定 + 防重启风暴 + 版本指纹** | ✅ 已实现并实测（2026-09-25）                | 后端 §7.10 / 前端 §6.2、§6.3                                   | `GET /api/code-status` + `POST /api/apply`（六分支 none/reload/static/restart/manual/blocked）；监视 12 文件（目录扫描自动纳入）；`data/run/restarts.json` 护栏 120s/3 次；`version` 指纹手写 FNV-1a（零新依赖）。**新增常驻进程/线程 0、新增内存 ≈0.05 MB**；需重启一次生效（见 #15） | ok   | ✅  | 已确认。实测六分支全通（none / reload 122ms / static / restart 3.4s / force+warning / blocked retry_after=110s）；新增 **0 进程 0 线程、≈0.05MB、0 依赖**。**待 #15 执行后启用**。 |

## 四、与 Phase 2（油猴 D）的边界（本清单**不含**）

- 油猴 D 全部能力（骨架 / IndexedDB 双存储 / 引擎 JS 移植 / 原生页 overlay / 浮动抽屉）仍为 **Phase 2 未动手**，见 `方案-油猴D.md` §0.1——按你要求，本清单不列。

---

## 五、由本轮标注派生的事项（新增，编号从 #20 起）

> 这些不是新需求，而是**你的标注直接推出**的可执行动作。每项都可独立授权执行。

| #  | 事项                            | 派生自 | 类型     | 具体动作                                                                                                                                        | 依赖        | 你的标注 |
| -- | ----------------------------- | --- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ---- |
| 20 | **② 自动回退分支现场复现**              | #3  | 验证     | 把 `ANALYZER_DB` 临时指向空库 → 触发 ② → 确认自动转全量 + 响应标注回退原因 → 还原                                                                                     | 无         |      |
| 21 | **数据源主开关 + 两模式数据规则**          | #2  | 功能     | `data_source.mode = auto/analyzer/local` + `GET·POST /api/data-source` + 设置弹窗三选一；切换即时 `reload_raw()`                                        | #27（判定函数） |      |
| 22 | **同 bvid 折叠 P0 修复：状态聚合**      | #5  | **修复** | 改 `_fold_latest_by_bvid` 为聚合版（`progress` 取 −1 优先；新增 `session_count`/`first_view_at`/`kids`）+ `classify_record` 支持多键命中。**两项须同批**（否则 skip 漂移） | 无         |      |
| 23 | **图片批量下载中继 + 测试入口**           | #8  | 功能     | 中继 `/images/start` · `stop` · `status` · `clear` · `local` 共 5 端点 + 小面板；`/cover/` 改为本地优先、回退外链。**先 `year=2026&use_sessdata=false` 冒烟**（写盘操作） | 无         |      |
| 24 | **remark 展示 + 编辑接入**          | #7  | 功能     | 卡片展示 `remark`（字段已在内存）；编辑中继 `POST /history/update-remark`；独立「我的备注」页**延后**                                                                    | 无         |      |
| 25 | **导出一致化：中继 `/export/*`**      | #6  | 功能     | 中继 3 端点（`export_history` / `download_excel` / `download_db`）+ 设置里 2 个按钮。**不自写 Excel**                                                       | 无         |      |
| 26 | **备份保留策略（保留最近 N 份）**          | #4  | 功能     | 给 `data/backup/` 加清理逻辑（≈10 行）。**不做定时备份**（Analyzer 也没有）                                                                                      | 无         |      |
| 27 | **SESSDATA 健康横幅**             | #1  | 功能     | `GET /api/fetcher-health` 加 `sessdata` 字段（转发 Analyzer `/login/check`）；网页顶部横幅 3 态（正常 / Analyzer 不可达已降级 / 凭证失效）                               | 无         |      |
| 28 | **文档改名：消除「Fetcher API 直连」歧义** | #10 | 文档     | 后端 §0.1 / §2.3 该行改名为「直连 B站抓取（collector 自持凭证）」，并标注「当前走中继，非直连」                                                                                | 无         |      |
| 29 | **文档数字口径化（由 #18 提升）**         | #18 | 文档     | 实测数字改为「口径日期 + 生成脚本」，根治 4450→4458 这类漂移                                                                                                       | 无         |      |

**零依赖可立即开工的**：#20、#22、#23、#24、#25、#26、#27、#28、#29（#21 依赖 #27）。  
**建议顺序**：#28 → #27 → #22 → #29 → 其余。
